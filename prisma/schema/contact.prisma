model Contact {
  id        String   @id @unique @default(uuid())
  firstName String
  lastName  String?
  email     String
  phone     String
  message   String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
